# Pinned to versions verified in the dev venv (2026-06-04).
# Spec suggested older versions; actual installed versions are newer and working —
# pinning to those instead to match what is tested.
fastapi==0.134.0
uvicorn[standard]==0.41.0
pandas==3.0.1
pandas-ta==0.4.71b0
yfinance==1.2.0
yahooquery==2.4.1
requests==2.32.5
numpy==2.2.6
pydantic==2.12.5
